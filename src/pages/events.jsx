import React from 'react';
import { Typography } from '@mui/material';

const Events = () => {
  return (
    <div>
      <Typography variant="h4">Events</Typography>
      {/* Add list of events and functionality here */}
    </div>
  );
};

export default Events;

