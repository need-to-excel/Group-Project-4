import React from 'react';
import { Typography } from '@mui/material';

const Profile = () => {
    return (
        <div>
            <Typography variant="h4">User Profile</Typography>
            {/* Add profile information and functionality here */}
        </div>
    );
};

export default Profile;